#include "mmain.h"
/*
 * add - operation declared in py
 * @a: first operator
 * @b: second operator
 * Return: the result integer
 */
int add(int a, int b)
{
	return (a + b);
}
/*
 * sub - operation declared in py
 * @a: first operator
 * @b: second operator
 * Return: the result integer
 */
int sub(int a, int b)
{
        return (a - b);
}
/*
 * mul - operation declared in py
 * @a: first operator
 * @b: second operator
 * Return: the result integer
 */
int mul(int a, int b)
{
        return (a * b);
}
/*
 * div - operation declared in py
 * @a: first operator
 * @b: second operator
 * Return: the result integer
 */
int div(int a, int b)
{
        return (a / b);
}
/*
 * mod - operation declared in py
 * @a: first operator
 * @b: second operator
 * Return: the result integer
 */
int mod(int a, int b)
{
        return (a % b);
}
